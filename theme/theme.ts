import { extendTheme } from '@chakra-ui/react';

export default extendTheme({
    colors: {
        primary: 'red',
        secondary: 'blue',
    },
    styles: {
        global: {
            body: {
                backgroundColor: '#353535',
            },
        },
    },
});
